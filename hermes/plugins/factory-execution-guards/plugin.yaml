name: factory-execution-guards
version: 0.11.0
description: Fail-closed supervised Claude execution, board-bound schema-2 handoff, atomic approval, and downstream byte revalidation.
entrypoint: __init__:register
provides_tools:
  - factory_review_approve
provides_hooks:
  - pre_tool_call
  - post_tool_call
