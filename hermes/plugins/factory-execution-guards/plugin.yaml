name: factory-execution-guards
version: 0.10.0
description: Fail-closed Claude execution with active-run authorization, schema-6 attestation, schema-1 handoff sealing, process-exit proof, and reviewer byte binding.
entrypoint: __init__:register
provides_hooks:
  - pre_tool_call
  - post_tool_call
