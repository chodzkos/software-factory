name: factory-execution-guards
version: 0.8.0
description: Fail-closed execution, atomic review claims, injection-safe Claude permissions, collision-free content attestation, sanitized Git state, and gated live-state provenance for privileged Software Factory profiles.
entrypoint: __init__:register
provides_hooks:
  - pre_tool_call
  - post_tool_call
