name: factory-repository-readonly
version: 0.3.0
description: Workspace-bound read-only repository tools for analyst, reviewer, and release-decision profiles.
provides_tools:
  - factory_repo_map
  - factory_repo_read
  - factory_repo_search
provides_hooks:
  - pre_tool_call
