name: factory-repository-readonly
version: 0.2.0
description: Workspace-bound read-only repository analysis tools and Kanban completion guard for Software Factory repository-analyst.
provides_tools:
  - factory_repo_map
  - factory_repo_read
  - factory_repo_search
provides_hooks:
  - pre_tool_call
