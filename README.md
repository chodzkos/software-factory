# software-factory

Bootstrap repository for shared standards, agent contracts, project memory, and workflows used across `chodzkos/*` software projects.

> Bootstrap note: this seed commit exists only to establish `main` so the full repository bootstrap can be delivered through a normal branch + pull request workflow.
